---
import { t } from '../i18n';
import { sections } from '../data/menu';

const links = [
  { href: '#menu', key: 'navMenu' as const },
  { href: '#room', key: 'navPlace' as const },
  { href: '#brew', key: 'navBrew' as const },
  { href: '#book', key: 'navBook' as const },
  { href: '#visit', key: 'navVisit' as const },
];
---

<nav class="rail" aria-label="SAVVA" data-rail>
  <a href="#top" class="rail-brand">
    <span class="rail-brand-ar">ساڤا</span>
    <span class="rail-brand-en">SAVVA</span>
  </a>

  <div class="rail-scroll">
    <ul class="rail-links" role="list">
      {
        links.map((l) => (
          <li>
            <a href={l.href} class="rail-link" data-rail-link={l.href}>
              <span data-lang="ar">{t[l.key].ar}</span>
              <span data-lang="en">{t[l.key].en}</span>
            </a>
          </li>
        ))
      }
    </ul>

    <ul class="rail-courses" role="list">
      {
        sections.map((s) => (
          <li>
            <a href={`#s-${s.id}`} class="rail-link" data-rail-link={`#s-${s.id}`}>
              <span data-lang="ar">{s.title.ar}</span>
              <span data-lang="en">{s.title.en}</span>
            </a>
          </li>
        ))
      }
    </ul>
  </div>

  <div class="rail-actions">
    <button
      type="button"
      class="rail-lang"
      data-lang-switch
      data-label-ar={t.switchLabel.ar}
      data-label-en={t.switchLabel.en}
      aria-label={t.switchLabel.ar}
    >
      <span data-lang="ar">{t.switchTo.ar}</span>
      <span data-lang="en">{t.switchTo.en}</span>
    </button>

    <button
      type="button"
      class="rail-cart"
      data-cart-open
      data-label-ar={t.orderOpen.ar}
      data-label-en={t.orderOpen.en}
      aria-label={t.orderOpen.ar}
    >
      <span data-lang="ar">{t.orderTitle.ar}</span>
      <span data-lang="en">{t.orderTitle.en}</span>
      <span class="rail-count tnum" data-cart-count hidden>0</span>
    </button>
  </div>
</nav>

<style>
  .rail {
    position: fixed;
    inset-block-start: 0;
    inset-inline: 0;
    z-index: 30;
    block-size: var(--rail-h);
    display: flex;
    align-items: center;
    gap: clamp(0.75rem, 2vw, 2rem);
    padding-inline: clamp(1.1rem, 4vw, 3rem);
    background: color-mix(in srgb, var(--color-ink) 78%, transparent);
    backdrop-filter: blur(16px) saturate(1.1);
    -webkit-backdrop-filter: blur(16px) saturate(1.1);
    border-block-end: 1px solid var(--sand-line-soft);
    transition: background-color 0.4s var(--ease-drift), border-color 0.4s var(--ease-drift);
  }
  /* Над фото первого экрана панель ещё прозрачнее; после ухода экрана — плотнее */
  .rail[data-scrolled='true'] {
    background: color-mix(in srgb, var(--color-ink) 92%, transparent);
    border-block-end-color: var(--sand-line);
  }

  .rail-brand { display: flex; align-items: baseline; gap: 0.55rem; flex: none; }
  .rail-brand-ar { font-family: var(--font-arabic-display); font-size: 1.1rem; }
  .rail-brand-en {
    font-family: var(--font-latin);
    font-size: 0.7rem;
    letter-spacing: 0.26em;
    color: var(--color-sage-bright);
  }

  .rail-scroll {
    flex: 1 1 auto;
    min-inline-size: 0;
    display: flex;
    align-items: center;
    gap: clamp(0.75rem, 2vw, 1.75rem);
    overflow-x: auto;
    scrollbar-width: none;
    -webkit-overflow-scrolling: touch;
  }
  .rail-scroll::-webkit-scrollbar { display: none; }

  .rail-links, .rail-courses {
    display: flex;
    align-items: center;
    gap: clamp(0.75rem, 1.6vw, 1.5rem);
    list-style: none;
    margin: 0;
    padding: 0;
    flex: none;
  }
  .rail-courses { display: none; }

  .rail-link {
    white-space: nowrap;
    font-size: 0.82rem;
    color: var(--sand-dim);
    padding-block: 0.35rem;
    border-block-end: 1px solid transparent;
    transition: color 0.3s var(--ease-drift), border-color 0.3s var(--ease-drift);
  }
  .rail-link:hover { color: var(--color-sand); }
  .rail-link[aria-current='true'] { color: var(--color-glow); border-block-end-color: var(--color-glow); }

  .rail-actions { display: flex; align-items: center; gap: 0.5rem; flex: none; }

  .rail-lang {
    font-size: 0.78rem;
    letter-spacing: 0.06em;
    color: var(--sand-dim);
    padding: 0.4rem 0.6rem;
    border: 1px solid transparent;
    transition: color 0.3s var(--ease-drift), border-color 0.3s var(--ease-drift);
  }
  .rail-lang:hover { color: var(--color-sand); border-color: var(--sand-line); }

  .rail-cart {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.8rem;
    padding: 0.45rem 0.9rem;
    border: 1px solid var(--sand-line);
    color: var(--color-sand);
    transition: background-color 0.3s var(--ease-drift), border-color 0.3s var(--ease-drift);
  }
  .rail-cart:hover { background: color-mix(in srgb, var(--color-sand) 10%, transparent); border-color: var(--sand-dim); }

  .rail-count {
    min-inline-size: 1.3rem;
    padding-inline: 0.3rem;
    text-align: center;
    background: var(--color-glow);
    color: var(--color-ink);
    font-size: 0.72rem;
    line-height: 1.35;
    font-weight: 600;
  }

  @media (max-width: 860px) {
    .rail-links { display: none; }
    .rail-courses { display: flex; }
    .rail-cart { padding-inline: 0.7rem; font-size: 0.75rem; }
    .rail-count[hidden] { display: none; }
  }
</style>
